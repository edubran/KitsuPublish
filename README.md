# KitsuPublish
Open-source Kitsu integration tools for VFX and animation pipelines. Includes a task panel (view assignments, change status, add comments) and a publisher (upload files, log time, notify Discord) — all configured via a single JSON file, no hardcoded values.
